# web — 0Gora chat UI (Next.js)

**Placeholder (v0.1.0 scaffold).** The Next.js chat UI — messages, streaming, citations, and the
**"Verified on 0G"** badge (read from the backend's `x_0g_verification`) — is implemented in **task 0.8**.

Planned: Next.js (App Router) + a model picker (from `/models`) + a public **Contribute** form (task 0.9).
